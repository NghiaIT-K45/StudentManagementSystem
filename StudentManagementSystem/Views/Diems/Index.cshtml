@model IEnumerable<StudentManagementSystem.Models.Diem>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SinhVien)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MonHoc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SoDiem)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NgayCapNhat)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SinhVien.TenSV)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MonHoc.TenMonHoc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SoDiem)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NgayCapNhat)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaDiem">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaDiem">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaDiem">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
